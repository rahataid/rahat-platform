meta {
  name: Activity Trigger Communication Stats
  type: http
  seq: 14
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "ms.activities.communication.getStats",
    "payload": {
    }
  }
}
