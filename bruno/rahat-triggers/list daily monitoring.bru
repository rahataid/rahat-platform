meta {
  name: list daily monitoring
  type: http
  seq: 18
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "ms.dailyMonitoring.getAll",
    "payload": {
       "page" : 1,
      "perPage": 10,
      "appId" : "{{appId}}"
    }
  }
}
