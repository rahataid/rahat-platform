meta {
  name: phase  getAll
  type: http
  seq: 4
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "ms.phases.getAll",
    "payload": {
      "appId": "{{appId}}"
    }
  }
}
