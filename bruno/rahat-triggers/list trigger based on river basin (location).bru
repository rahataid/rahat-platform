meta {
  name: list trigger based on river basin (location)
  type: http
  seq: 9
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "ms.triggers.getByLocation",
    "payload": {
       "page" : 1,
      "perPage": 10,
      "location" :"bheri"
    }
  }
}
