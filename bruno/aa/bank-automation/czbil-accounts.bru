meta {
  name: czbil-accounts
  type: http
  seq: 3
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "aa.bank-scrape.czbil.accounts",
    "payload": {
      "username": "custom_user",
      "password": "custom_pass",
      "totp": "123456"
    }
  }
}

script:post-response {
  const responseData = res.getBody();
  console.log('CZBIL Accounts Response:', responseData);
}
