meta {
  name: trigger payout
  type: http
  seq: 6
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "aa.payout.triggerPayout",
    "payload": {
      "uuid": "9f6df560-490f-4e8f-9940-048afe94c861"
    }
  }
}

script:post-response {
  const responseData = res.getBody();
  bru.setVar("categoryId", responseData.data?.uuid);
}
