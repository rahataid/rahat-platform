meta {
  name: get payout log
  type: http
  seq: 7
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "aa.payout.getPayoutLog",
    "payload": {
      "uuid": "9345e93e-e033-4b9c-83ab-a9ba874ad876"
    }
  }
}

script:post-response {
  const responseData = res.getBody();
  bru.setVar("categoryId", responseData.data?.uuid);
}
