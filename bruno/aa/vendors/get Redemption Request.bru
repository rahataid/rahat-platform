meta {
  name: get Redemption Request
  type: http
  seq: 2
}

post {
  url: {{url}}/projects/{{uuid}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "aa.stellar.getRedemptionRequest",
    "payload": {
      "uuid": "e0207376-df9e-41cc-8614-00075e87888e"
      }
  }
}

script:post-response {
  const responseData = res.getBody();
  bru.setVar("categoryId", responseData.data?.uuid);
}
