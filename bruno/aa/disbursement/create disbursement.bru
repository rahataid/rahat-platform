meta {
  name: create disbursement
  type: http
  seq: 1
}

post {
  url: {{url}}/projects/{{project_id}}/actions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "action": "aa.chain.disburse",
    "payload": {
       "dName":"2026"
    }
  }
  
}

script:post-response {
  const responseData = res.getBody();
  bru.setVar("categoryId", responseData.data?.uuid);
}
