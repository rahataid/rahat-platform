meta {
  name: Balances
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/v1/payout/balances
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

tests {
  // set the access token
  bru.setVar('merchant_key', res.getBody()[0].merchant_key);
}
