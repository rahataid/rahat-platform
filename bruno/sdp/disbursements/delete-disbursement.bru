meta {
  name: delete-disbursement
  type: http
  seq: 4
}

delete {
  url: {{sdp_url}}/disbursements/{{disbursement_id}}
  body: none
  auth: bearer
}

headers {
  SDP-Tenant-Name: {{sdp_tenant_name}}
}

auth:bearer {
  token: {{sdp_access_token}}
}
