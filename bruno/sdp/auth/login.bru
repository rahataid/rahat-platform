meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{sdp_url}}/login
  body: json
  auth: none
}

headers {
  SDP-Tenant-Name: {{sdp_tenant_name}}
}

body:json {
  {
    "email": "manjik.shrestha@rumsan.net",
    "password": "Onchain@9670"
  }
}

script:post-response {
  const body = res.getBody();
  if (body.token) {
    bru.setEnvVar("sdp_access_token", body.token);
  }
}
