meta {
  name: update-receiver-wallet-status
  type: http
  seq: 6
}

patch {
  url: {{sdp_url}}/receivers/wallets/{{receiver_wallet_id}}/status
  body: json
  auth: bearer
}

headers {
  SDP-Tenant-Name: {{sdp_tenant_name}}
}

auth:bearer {
  token: {{sdp_access_token}}
}

body:json {
  {
    "status": "READY"
  }
}
