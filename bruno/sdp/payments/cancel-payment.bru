meta {
  name: cancel-payment
  type: http
  seq: 5
}

patch {
  url: {{sdp_url}}/payments/{{payment_id}}/status
  body: json
  auth: bearer
}

headers {
  SDP-Tenant-Name: {{sdp_tenant_name}}
}

auth:bearer {
  token: {{sdp_api_key}}
}

body:json {
  {
    "status": "CANCELED"
  }
}
