meta {
  name: stress-testing active
  type: http
  seq: 3
}

get {
  url: {{url}}/stress-test/active
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

script:post-response {
  const responseData = res.getBody();
  bru.setVar("categoryId", responseData.data?.uuid);
}
