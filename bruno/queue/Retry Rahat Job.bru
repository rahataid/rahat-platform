
meta {
  name: retry-rahat-job
  type: http
  seq: 4
}

get {
  url: {{url}}/queue/rahat-jobs/retry/2
  auth: none
}

assert {
  res.status: eq 200
  res.body.message: contains 'retried successfully'
}

tests {
  test('should confirm rahat job retry', async () => {
    const message = res.getBody().message;
    expect(message).to.include('retried successfully');
  });
}
